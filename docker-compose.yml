services:
  cyberpunk-red-support:
    build:
      context: .
      args:
        VITE_API_URL: https://itl-app.vps-kinghost.net/api
    image: cyberpunk-red-support:latest
    ports:
      - "127.0.0.1:8080:8080"
    restart: unless-stopped
